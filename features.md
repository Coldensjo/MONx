- In the Items ThingBrowser
A new section under it should be added called Loot
A user can click on an item to select it (exactly as its working right now)
A user can ctrl + click on an item to select it, or more items if they click on more than 1 item
A user can hold alt and drag to create a selection square to select items
A user can shift + click on an item to select that item + click on another item after or before to select all items between them
A user can drag their mouse across multiple items to select all of them (draging across same item does not deselect them)
A user can rightclick on any selected item and add it to Loot
A button on the right side then lets the user "Add loot to monster x"


- Right click on an item to "set as corpse for monster x"