# MONx — Suggested features & improvements

Shipped entries are removed rather than struck through — `git log` is the
record of what landed. Effort: S (hours), M (a day-ish), L (multi-day).

Nothing is currently queued.
